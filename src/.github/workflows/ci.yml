name: "React Ci Pipeline"

on:
    push:
        branches:
            - main

jobs:
    buils-and-test:
        runs-on: ubuntu-latest

        steps:
            - name: "Setup Node js"
              uses: actions/setup-node@v4
              with:
                node-version: '21'

