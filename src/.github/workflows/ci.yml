name: "React Ci Pipeline"

on:
    push:
        branches:
            - main
            - master

jobs:
    runs-on: ubuntu-latest
    
    steps:
        - name: "setup Node js"
          uses: actions/setup-node@v4
          with:
            node-version: '21'
